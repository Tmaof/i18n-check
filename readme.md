# i18n-check

一个国际化检查和处理工具，用于自动检测代码中的中文文本，并自动添加 `i18n.t()` 包裹，同时支持自动导入 i18n 模块和 AI 翻译处理。

## 📦 项目结构

这是一个 monorepo 项目，使用 pnpm workspace 管理：

```
i18n-check/
├── i18n-check/          # 核心库
│   └── readme.md         # 详细文档和 API 说明
├── demo-react/          # React 示例项目
│   └── readme.md         # React 使用示例
├── demo-vue3/           # Vue 3 示例项目
│   └── readme.md         # Vue 3 使用示例
├── test/                # 测试用例
└── build/               # 构建工具
```

## 🚀 快速开始

### 安装依赖

```bash
pnpm install
```

### 运行示例项目

**React 示例：**

```bash
pnpm start:demo-react
```

**Vue 3 示例：**

```bash
pnpm start:demo-vue3
```

### 运行测试

```bash
pnpm test
```

### 构建项目

```bash
pnpm build
```

## 📖 文档

- **[核心库文档](./i18n-check/readme.md)** - 完整的 API 文档和使用指南
- **[React 示例](./demo-react/readme.md)** - React 项目使用示例
- **[Vue 3 示例](./demo-vue3/readme.md)** - Vue 3 项目使用示例

## 🛠️ 开发

### 代码检查

```bash
# 代码格式化
pnpm prettier

# ESLint 检查
pnpm lint:script

# 自动修复
pnpm lint-fix
```

### 安装核心库

```bash
npm install i18n-check
# 或
pnpm add i18n-check
# 或
yarn add i18n-check
```

## ✨ 主要特性

- 🔍 自动检测代码中未被 `i18n.t()` 包裹的中文文本
- 🎯 自动为中文文本添加 `i18n.t()` 包裹
- 📦 自动检测并导入 i18n 模块
- 🚫 智能忽略注释、枚举等不需要翻译的文本
- 📝 自动标记包含中文的模板字符串
- 🔑 提取所有 `i18n.t()` 中的文本 key
- 🤖 支持调用 LLM API 进行批量翻译

## 📄 许可证

MIT
